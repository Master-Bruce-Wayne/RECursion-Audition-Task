import React from 'react'
import { useAuth } from '../context/UserContext.jsx'

const Profile = () => {
  return (
    <div></div>   // Display all the userData from useAuth and a logout button
  )
}

export default Profile